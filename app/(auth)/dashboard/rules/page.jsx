'use client'

import RulesPanel from '@/app/components/RulesPanel'

// The full-page Rules view is now the shared RulesPanel (also embedded as a
// collapsible panel inside the Rota Builder).
export default function RulesPage() {
  return <RulesPanel />
}
